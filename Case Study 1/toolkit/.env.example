# Copy to .env and fill in, or just export the variable in your shell.
#
# Only needed for the live commands:
#   python run_all.py --measure        (Phase 8 ladder + Phase 7 probe)
#   python run_all.py --chain --live   (Phase 10 seven-step chain)
#
# Building the report does NOT need this.
ANTHROPIC_API_KEY=sk-ant-...
